# MCP Station — Environment Configuration
# Copy this to .env and fill in your values

# Port the web UI and API listen on (default: 3000)
PORT=3000

# Bearer token for API authentication
# Set to any random string - leave as 'changeme' only for local dev
AUTH_TOKEN=changeme

# TrueNAS dataset path where config & logs are persisted
# This should be a path to a dataset on your TrueNAS pool
DATA_PATH=/mnt/data/mcp-station

# (Optional) Path to expose to the filesystem MCP server
FILES_PATH=/mnt/data/files
